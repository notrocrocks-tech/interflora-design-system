# macOS
.DS_Store

# Reference screenshots — layout-only, excluded from the repo.
# The brand book PDF in uploads/ is kept deliberately.
uploads/*.png
uploads/*.jpg
uploads/*.jpeg
uploads/*.webp

# Dependencies (if any tooling is added later)
node_modules/

# design-sync build output — regenerated by `npm run check`, not source
ds-bundle/
.render-check/

# Local env
.env
.env.local
